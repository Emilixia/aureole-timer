(function() {
  'use strict';
  // Make require('three') return the global THREE
  var _orig = typeof require !== 'undefined' ? require : null;
  var _fakeRequire = function(mod) {
    if (mod === 'three') return THREE;
    if (_orig) return _orig(mod);
    throw new Error('Cannot require ' + mod);
  };
var __GLTF_BUNDLE__ = (() => {
  var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
    get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
  }) : x)(function(x) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x + '" is not supported');
  });

  // ../../../../../tmp/gltf_bundle_entry.js
  var import_GLTFLoader = __require("three/examples/jsm/loaders/GLTFLoader.js");
  window.GLTFLoader = import_GLTFLoader.GLTFLoader;
})();

  if (typeof __GLTF_BUNDLE__ !== 'undefined' && __GLTF_BUNDLE__.GLTFLoader) {
    window.GLTFLoader = __GLTF_BUNDLE__.GLTFLoader;
  }
})();
